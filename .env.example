# QueueStorm Investigator — environment variables (names only, no secrets).
# The service is a pure deterministic rule engine: there are NO secrets or API keys.

# Port the server binds to. Render injects this automatically; defaults to 8000 locally.
PORT=8000
